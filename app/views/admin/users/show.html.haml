.profile
  .row
    .col-md-4
      %h1
        = user.firstname
        = user.lastname
      %p
        = user.email
      -if (current_user.has_role? :headmaster) || (current_user.id == user.id)    
        = link_to "Edit profile", edit_admin_user_path(user)
